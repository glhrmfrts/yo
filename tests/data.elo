// test data structures

fmt, strings := import("fmt"), import("strings")

func Person.add(other) {
  fmt.println(this.name, other.name)
}

func Person.sub(other) {
  other.marry(this)
  return this, other.call()
}

strings.repeat(s, 35)

return {
  Person: Person,
}